{
  "name": "define",
  "version": "1.0.0",
  "description": "slack slash command to define words",
  "main": "server.js",
  "scripts": {
    "start": ". ./credentials.sh && babel-node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "slack-integration"
  ],
  "author": "blovato",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "fetch": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0"
  }
}
